#include "template.inc"
#include "files.inc"
#include "iovars.inc"

/*
		-------------------- 
		| System Variables |
		--------------------
*/
struct sysvars SYS;	/* The system variables generated by gendb */





/*
		---------------------------
		| Logical Field Structure |
		---------------------------
*/

struct logfield FIELD[MAXNLFLDS];



char TAGBUF[TAGBUFSZ];		/* The buffer to hold tags */


/*
			-----------------
			| Screen Buffer |
			-----------------
*/
char SCREEN[SCREENHT][SCREENWD];




/*
			----------------------
			|  Common Variables  |
			----------------------
*/

int DBFD;		/* File descriptor for data base file */
unsigned NEXTSECTOR;	/* Next free disk record on db file */
int NENTRIES;		/* Number of entries since last back-up */
char BACKUPFLAG;	/* Flag indicating whether to back up to disk */
struct _date TODAY;	/* Today's date */
char DBDIR[MAXFNSZ];	/* The directory on which the data base is found */
  


/*
	----------------------
	| Months of the Year |
	----------------------
*/
char MNBUF[90];			/* Buffer holding characters */
char *MNSTR[12];		/* Pointers to months */
char DAYSPERMN[12];		/* Days per month */


